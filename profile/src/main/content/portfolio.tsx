import { Grid } from "@mui/material";

export default function Portfolio() {
    return(
        <Grid container spacing={0}>
            <Grid item xs={8}>
                Portfolio
            </Grid>
        </Grid>
    );
}
